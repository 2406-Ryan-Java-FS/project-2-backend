
# H2 Database Configuration
spring.datasource.url=jdbc:h2:mem:testdb;INIT=RUNSCRIPT FROM 'classpath:schema.sql'
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.datasource.platform=h2

# Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=create
spring.datasource.schema=classpath:schema.sql


# H2 Console Configuration (Optional)
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console

#Gitbash or linux will have openssl commands to generate a random secret key
#openssl rand -base64 512
#That random key can then be set in a system environment variable
#Reload your gitbash, cmd line, or terminal so it's brought in
jwt.secret=${JWT_SECRET}